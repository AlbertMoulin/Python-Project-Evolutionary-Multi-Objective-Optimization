This is a Python project on Multi Objective Optimisation
It implements the NSGA II Algorithme and it runs it on the LOTZ and mLOTZ functions.
It also implements a modified version of the algorithm which recalculates the crowding distances for every tie broken. The modified version can be found in the 'ModifiedVersion' branch
